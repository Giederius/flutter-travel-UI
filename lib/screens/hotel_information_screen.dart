import 'package:flutter/material.dart';

class HotelInformationScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Text('Boom'),
        ],
      ),
    );
  }
}
